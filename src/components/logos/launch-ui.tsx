const LaunchUI = (props: React.ImgHTMLAttributes<HTMLImageElement>) => (
  <img
    src="./icon.svg"
    width={24}
    height={24}
    alt="Launch UI Logo"
    {...props}
  />
);

export default LaunchUI;
